node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Local tooling (reinstalled via `npx impeccable install`, not project source)
.claude/skills/
.claude/settings.local.json
.impeccable/

# Keys & secrets — must never be committed
.env
.env.*
!.env.example
*.pem
*.key

# Singgahan jawaban Overpass — hanya untuk mempermudah percobaan ulang.
scripts/.overpass-cache/

# Scratch directory the gstack browse tooling writes into.
.gstack/

# Python bytecode from the PRD builder.
__pycache__/
*.pyc
