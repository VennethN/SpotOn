"""Builder for the MAPID WebGIS Competition 2026 PRD.

Split the way the document is: `theme` holds the template's measurements,
`layout` typesets, `diagrams` draws, `figures` reads every number out of the
built grid, and `content` is the copy. Run it through `scripts/build-prd.py`.
"""
